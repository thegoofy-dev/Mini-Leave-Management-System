#!/bin/bash
# Simple seed: run the server and then use the API to create sample data via curl or APIDOG.
echo "Start the server (python app.py) then import apidog_collection.json into Apidog or run the sample requests."
